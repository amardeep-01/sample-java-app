name: Deploy Servlet App

on:
  push:
    branches: [dev,main]
  pull_request:
    branches: [dev,main]
jobs:
   deploy:
    runs-on:  [self-hosted]

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Setup JDK
      uses: actions/setup-java@v2
      with:
           java-version: '11'
           distribution: 'adopt'
           java-package: 'jdk'
           architecture: 'x64'
           check-latest: false
           server-id: 'github'
           server-username: ${{ github.actor }}
           server-password: ${{ secrets.GITHUB_TOKEN }}
           overwrite-settings: true
           job-status: 'success'

    - name: Build with Maven
      run: mvn clean package
      
    # - name: Install Maven
    #   run: |
    #      Invoke-WebRequest -Uri "https://apache.mirror.digitalpacific.com.au/maven/maven-3/3.8.4/binaries/apache-maven-3.8.4-bin.zip" -OutFile "apache-maven-3.8.4-bin.zip" -UseBasicParsing
    #      Expand-Archive -Path apache-maven-3.8.4-bin.zip -DestinationPath "C:\"
    #       mv "C:\apache-maven-3.8.4" "C:\Maven"
    #       $env:PATH = "C:\Maven\bin;$env:PATH"

    # - name: Build with Maven
    #   run: mvn clean package

    - name: Copy WAR file to IIS directory
      run: |
        cd ../../../../../..
        dir
        cd target
        dir
        del "C:/java/sample-0.0.1-SNAPSHOT.jar"
        Copy-Item -Path "D:\a\sample-java-app\sample-java-app\target\  java -jar sample-0.0.1-SNAPSHOT.jar" -Destination "C:/java" -Force
          
        
    - name: Restart IIS
      run: |
        # Restart IIS to apply changes
